# Test / load-test tooling only (LIT-203 requirements split). Production
# images install requirements.txt alone, so pytest/fakeredis/locust and the
# test-only httpx/mongomock never end up in a shipped image.
-r requirements.txt

httpx==0.27.0
# >=8.3.3 is locust's floor (it depends on pytest); the exact pin below it
# made `pip install -r requirements.txt` unresolvable once locust was added.
# Verified on 8.4.2: backend suite 587 passed, 6 skipped.
pytest>=8.3.3,<9
pytest-asyncio==0.23.7
fakeredis==2.23.2
mongomock>=4.1
locust>=2.31,<3.0