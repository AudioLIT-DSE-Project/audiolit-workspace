fastapi==0.111.0
uvicorn[standard]==0.30.0
redis==5.0.7
msgpack>=1.0,<2.0
rq==2.10.0
lz4>=4.0,<5.0
pydantic-settings==2.3.4
httpx==0.27.0
# >=8.3.3 is locust's floor (it depends on pytest); the exact pin below it
# made `pip install -r requirements.txt` unresolvable once locust was added.
# Verified on 8.4.2: backend suite 587 passed, 6 skipped.
pytest>=8.3.3,<9
pytest-asyncio==0.23.7
fakeredis==2.23.2
transformers>=4.30.0
torch>=2.6.0
librosa>=0.10.0
soundfile>=0.12.0
python-multipart>=0.0.6
scikit-learn>=1.3.0
umap-learn>=0.5.3
numpy>=1.24,<2.0
pandas
captum>=0.6.0
accelerate
jiwer>=4.0.0
locust>=2.31,<3.0
# SRS §3.10 / SAD §9 — durable MongoDB metadata tier (LIT-255/256/257). The
# pymongo driver is pure-Python, no native build; mongomock backs the unit
# tests that don't need a live server (mirrors fakeredis).
pymongo>=4.6,<5.0
mongomock>=4.1
